// Helper functions like:
export const validateServiceData = (data) => {
  // Validation logic
};

export const getDefaultServiceData = () => {
  // Returns properly initialized service object
};

export const processServiceForDisplay = (service) => {
  // Ensures all fields exist with defaults
};